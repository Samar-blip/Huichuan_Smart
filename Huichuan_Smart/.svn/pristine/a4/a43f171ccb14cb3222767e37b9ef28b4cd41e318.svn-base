namespace Application.JwtService
{
    //JWT Token 服务接口
    public interface IJwtService
    {
        //生成 JWT Access Token（包含用户ID、用户名、角色ID）
        string GenerateToken(long userId, string userName, long roleId);
    }
}
